{% if modules %}
    <div id="{{ page_type }}"

    class="col-sm-3 hidden-xs side-column {% if journal2.settings.get('flyout_column_left_active') %} flyout-left {% endif %}">
        {% for module in modules %}
            {{ module }}
        {% endfor %}

    </div>
{% endif %}
