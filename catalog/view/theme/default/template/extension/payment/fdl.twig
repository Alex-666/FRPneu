{% if url_confirm %}

<div class="buttons">
  <div class="pull-right">
      <a href="{{ url_confirm }}" id="button-confirm" class="btn btn-primary">{{ button_confirm }}</a>
  </div>
</div>

{% endif %}

